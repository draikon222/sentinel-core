{
  "name": "sentinel-core",
  "version": "1.0.0",
  "description": "Sentinel Core Interface",
  "main": "client.js",
  "scripts": {
    "start": "node client.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
